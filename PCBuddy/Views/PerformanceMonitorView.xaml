<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PCBuddy.Views.PerformanceMonitorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="700" d:DesignWidth="900"
    Background="#FF121812">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Padding="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Performance Monitor" FontSize="28" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,8"/>
                    <TextBlock Text="Real-time system resource usage" Foreground="#B7B9B5"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="StartStopButton" Content="Start Monitoring" Padding="16,8" Click="StartStopButton_Click"/>
                    <TextBlock x:Name="StatusText" Text="Stopped" Foreground="#9E9E9E" VerticalAlignment="Center" Margin="12,0,0,0"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Margin="0,24,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="#1E1E1E" CornerRadius="12" Padding="20" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="CPU" FontSize="14" Foreground="#B7B9B5" FontWeight="SemiBold"/>
                        <Grid Margin="0,12,0,0">
                            <ProgressBar x:Name="CpuProgress" Height="12" Foreground="#4FC3F7" Background="#333"/>
                            <TextBlock x:Name="CpuPercentText" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Foreground="White"/>
                        </Grid>
                        <TextBlock x:Name="CpuTempText" Text="Temp: --" Foreground="#9E9E9E" FontSize="12" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Background="#1E1E1E" CornerRadius="12" Padding="20" Margin="8,0,8,0">
                    <StackPanel>
                        <TextBlock Text="RAM" FontSize="14" Foreground="#B7B9B5" FontWeight="SemiBold"/>
                        <Grid Margin="0,12,0,0">
                            <ProgressBar x:Name="RamProgress" Height="12" Foreground="#81C784" Background="#333"/>
                            <TextBlock x:Name="RamPercentText" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Foreground="White"/>
                        </Grid>
                        <TextBlock x:Name="RamUsedText" Text="Used: --" Foreground="#9E9E9E" FontSize="12" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Background="#1E1E1E" CornerRadius="12" Padding="20" Margin="8,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Disk" FontSize="14" Foreground="#B7B9B5" FontWeight="SemiBold"/>
                        <Grid Margin="0,12,0,0">
                            <ProgressBar x:Name="DiskProgress" Height="12" Foreground="#FFB74D" Background="#333"/>
                            <TextBlock x:Name="DiskPercentText" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Foreground="White"/>
                        </Grid>
                        <TextBlock x:Name="DiskUsedText" Text="Total Disk Usage" Foreground="#9E9E9E" FontSize="12" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Grid Grid.Row="2" Margin="0,16,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="#1E1E1E" CornerRadius="12" Padding="20" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="GPU(s)" FontSize="14" Foreground="#B7B9B5" FontWeight="SemiBold"/>
                        <ItemsControl x:Name="GpuListPanel" Margin="0,12,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#2A2A2A" CornerRadius="6" Padding="10,8" Margin="0,0,0,6">
                                        <StackPanel>
                                            <Grid>
                                                <TextBlock Text="{Binding Name}" Foreground="#FFFFFF" FontSize="12" TextWrapping="Wrap"/>
                                                <TextBlock Text="{Binding Usage}" Foreground="#BA68C8" FontSize="12" FontWeight="Bold" HorizontalAlignment="Right"/>
                                            </Grid>
                                            <ProgressBar Height="6" Value="{Binding UsageValue}" Maximum="100" Foreground="#BA68C8" Background="#444" Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Background="#1E1E1E" CornerRadius="12" Padding="20" Margin="8,0,0,0">
                    <StackPanel>
                        <TextBlock Text="Network" FontSize="14" Foreground="#B7B9B5" FontWeight="SemiBold"/>
                        <Grid Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Download" Foreground="#9E9E9E" FontSize="11"/>
                                <TextBlock x:Name="NetworkDownText" Text="0 MB/s" Foreground="#81C784" FontWeight="Bold" FontSize="18"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Upload" Foreground="#9E9E9E" FontSize="11"/>
                                <TextBlock x:Name="NetworkUpText" Text="0 MB/s" Foreground="#64B5F6" FontWeight="Bold" FontSize="18"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Row="3" Background="#1E1E1E" CornerRadius="12" Padding="20" Margin="0,16,0,0">
                <StackPanel>
                    <TextBlock Text="CPU Usage Over Time" FontSize="14" Foreground="#B7B9B5" FontWeight="SemiBold" Margin="0,0,0,12"/>
                    <Canvas x:Name="CpuGraph" Height="120" Background="#252525"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="4" Background="#1E1E1E" CornerRadius="12" Padding="20" Margin="0,16,0,0">
                <StackPanel>
                    <TextBlock Text="RAM Usage Over Time" FontSize="14" Foreground="#B7B9B5" FontWeight="SemiBold" Margin="0,0,0,12"/>
                    <Canvas x:Name="RamGraph" Height="120" Background="#252525"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="5" Background="#1E1E1E" CornerRadius="12" Padding="20" Margin="0,16,0,0">
                <StackPanel>
                    <TextBlock Text="Disk Usage Over Time" FontSize="14" Foreground="#B7B9B5" FontWeight="SemiBold" Margin="0,0,0,12"/>
                    <Canvas x:Name="DiskGraph" Height="120" Background="#252525"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
