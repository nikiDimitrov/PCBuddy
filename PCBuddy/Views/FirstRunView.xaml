<?xml version="1.0" encoding="utf-8"?>
<winex:WindowEx
    x:Class="PCBuddy.Views.FirstRunView"
    x:Name="root"
    xmlns:winex="using:WinUIEx"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PCBuddy.Views"
    xmlns:models="using:PCBuddy.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    MinWidth="250"
    MinHeight="750"
    Title="Welcome!">

    <winex:WindowEx.SystemBackdrop>
        <MicaBackdrop/>
    </winex:WindowEx.SystemBackdrop>

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
            Background="#FF121812"
            BorderBrush="#FF1A2013"
            BorderThickness="2"
            CornerRadius="10"
            Padding="24,18"
            Margin="0,0,0,12"
            HorizontalAlignment="Stretch">

            <TextBlock
                Text="{x:Bind ViewModel.WelcomeText, Mode=OneWay}"
                FontSize="18"
                FontFamily="Segoe UI"
                FontWeight="SemiBold"
                Foreground="#FFB7B9B5"
                TextWrapping="Wrap"
                CharacterSpacing="10" />
        </Border>

        <Grid 
            Grid.Row="1" 
            Margin="0,4,0,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                TextWrapping="Wrap"
                Text="{x:Bind ViewModel.ChooseProfileText, Mode=OneWay}"/>

            <ListView
                x:Name="ProfileView"
                Grid.Row="1"
                ItemsSource="{x:Bind ViewModel.UserProfiles}"
                SelectionMode="Single"
                SelectionChanged="{x:Bind ViewModel.ProfileView_SelectionChanged}">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid 
                            Orientation="Horizontal"
                            ItemWidth="200"
                            ItemHeight="200"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>

                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:UserProfile">
                        <StackPanel>
                            <Image 
                                Source="{x:Bind IconPath}" 
                                Width="100"
                                Height="100"/>
                            <TextBlock 
                                Text="{x:Bind DisplayName}"
                                HorizontalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <Button
            Grid.Row="2"
            HorizontalAlignment="Center"
            Width="200"
            Content="{x:Bind ViewModel.ApplyProfileButtonText, Mode=OneWay}">
        </Button>
    </Grid>
</winex:WindowEx>
